# =============================================================================
# Architecture Builder - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in the values for your environment

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_NAME=Architecture Builder API
APP_VERSION=1.0.0
DEBUG=false

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB connection string
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=architecture_builder

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT Secret - IMPORTANT: Change this to a secure random string in production!
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Password Reset Token Expiry
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=24

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Allowed origins for CORS (JSON array format)
CORS_ORIGINS=["http://localhost:4200","http://frontend:4200"]

# =============================================================================
# LLM PROVIDER CONFIGURATION
# =============================================================================
# Select which LLM provider to use: openai | azure | ollama
LLM_PROVIDER=openai

# =============================================================================
# OPENAI CONFIGURATION
# =============================================================================
# Your OpenAI API key - get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI API URL (default is the official API)
OPENAI_API_URL=https://api.openai.com/v1

# OpenAI Model to use
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# AZURE OPENAI CONFIGURATION
# =============================================================================
# Azure OpenAI API Key
AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# Azure OpenAI Endpoint (e.g., https://your-resource.openai.azure.com)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# Azure OpenAI API Version
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure OpenAI Deployment Name (the name you gave your deployment)
AZURE_OPENAI_DEPLOYMENT=gpt-5

# =============================================================================
# OLLAMA CONFIGURATION (Local LLM)
# =============================================================================
# Ollama server URL (default for local installation)
OLLAMA_BASE_URL=http://localhost:11434

# Ollama Model to use
# Popular options: llama3.2, llama3.1, mistral, codellama, mixtral
OLLAMA_MODEL=llama3.2

# =============================================================================
# LLM PARAMETERS
# =============================================================================
# Temperature for LLM responses (0.0 = deterministic, 1.0 = creative)
LLM_TEMPERATURE=0.7

# Maximum tokens for LLM responses
LLM_MAX_TOKENS=4096

# Number of retries for failed LLM calls
LLM_MAX_RETRIES=3

# Timeout for LLM calls in seconds
LLM_TIMEOUT=120

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# NOTES
# =============================================================================
# 
# Provider Selection Guide:
# 
# 1. OpenAI (LLM_PROVIDER=openai)
#    - Best for: Production use, highest quality responses
#    - Requires: OPENAI_API_KEY
#    - Cost: Pay per token
# 
# 2. Azure OpenAI (LLM_PROVIDER=azure)
#    - Best for: Enterprise use, data residency requirements
#    - Requires: AZURE_OPENAI_API_KEY, AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_DEPLOYMENT
#    - Cost: Pay per token (Azure pricing)
# 
# 3. Ollama (LLM_PROVIDER=ollama)
#    - Best for: Local development, privacy-sensitive use, no API costs
#    - Requires: Ollama installed locally (https://ollama.ai)
#    - Cost: Free (runs locally)
#    - Note: Requires more RAM/GPU for larger models
# 
# Quick Start with Ollama:
#   1. Install Ollama: curl -fsSL https://ollama.ai/install.sh | sh
#   2. Pull a model: ollama pull llama3.2
#   3. Set LLM_PROVIDER=ollama in your .env
#   4. Start your application
